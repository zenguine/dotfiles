Backup of Vim Configuration Files.  To use elsewhere, run:

Requirements: Must have vim compiled with ruby, and must have ruby-dev
package installed

> cd $HOME
> git clone https://github.com/phenguin/vimconfig .vim
> cd .vim
> ./vimsetup

